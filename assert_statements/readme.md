Son expresiones para el manejo de errores:

TEnemos un codigo y un assert statement, si esto se cumple vuelve al codigo, si no va hacia un error.

Su forma es:<br><br>
<b>assert condición, mensaje de error</b>